# Angebote (Kaufland/Edeka) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Angebote von Kaufland und Edeka (lokale Filiale per PLZ) im Add-on
anzeigen, Artikel hervorheben, die regelmäßig benötigt werden, und passende
Angebote als Gewichtsbonus in die Wochenplanung einfließen lassen.

**Architecture:** Neues Package `app/offers/` mit drei unabhängigen
Connectors (Kaufland-Scraper, Edeka-Scraper, Marktguru-API), die alle in
dieselbe `Offer`-Tabelle schreiben (getaggt mit `source`). Ein
Matching-Modul (rapidfuzz) verbindet Angebote mit Rezept-Zutaten sowie mit
`FridgeStaple`/`WatchlistItem`. Ein In-Process-Scheduler (APScheduler)
löst die Connectors nach individuell konfigurierbarem Zeitplan aus
(`OfferSourceConfig`). Der bestehende Planungsalgorithmus
(`planner.py`) bekommt einen zusätzlichen additiven Gewichtsbonus. Die
bestehende Single-Page-`index.html` bekommt einen neuen Tab "Angebote".

**Tech Stack:** FastAPI, SQLAlchemy/SQLite (bestehend), `rapidfuzz`
(Fuzzy-Matching, neu), `beautifulsoup4` (HTML-Parsing, neu), `APScheduler`
(Scheduling, neu), `pytest` + FastAPI `TestClient` (Test-Infrastruktur,
neu — existiert im Projekt bisher nicht), vanilla JS (bestehendes Muster
in `index.html`).

**Spec:** [docs/superpowers/specs/2026-09-04-angebote-design.md](../specs/2026-09-04-angebote-design.md)

## Global Constraints

- Bestehendes Grundgerüst wird erweitert, nicht umgeschrieben (siehe
  `essensplaner/CLAUDE.md`).
- App-Module in `essensplaner/app/` sind flache Module (kein Package,
  `WORKDIR /app` im Container) — Imports untereinander sind immer
  `import database`, `from models import X`, nie relative Package-Imports.
- Neue Tabellen entstehen automatisch über `Base.metadata.create_all()`
  (`database.init_db()`); neue Spalten auf bestehenden Tabellen (hier:
  `Settings`) müssen zusätzlich in `_migrate_add_missing_columns()`
  ergänzt werden, da `create_all()` bestehende Tabellen nicht verändert.
- Kein Merge zwischen Connector-Läufen: ein Lauf ersetzt alle
  `Offer`-Zeilen seines eigenen `source`-Werts vollständig (Angebote sind
  wöchentlich neu, siehe Spec).
- Jede neue Dependency wird in `essensplaner/requirements.txt` (Laufzeit)
  bzw. `essensplaner/requirements-dev.txt` (nur Tests) ergänzt, exakt
  gepinnt wie die bestehenden Einträge (`paket==version`).
- Scraper-Selektoren (Kaufland/Edeka) sind laut Spec ein bekanntes Risiko
  (Layout-Änderungen) — jeder Connector-Lauf schreibt Erfolg/Fehler in
  `OfferSourceConfig.last_status`, nie eine stille Exception.
- CI verlangt bei jeder Änderung einen Versionsbump in `config.yaml` +
  einen `CHANGELOG.md`-Eintrag (siehe `.github/scripts/check_changelog.sh`)
  — das übernimmt der letzte Task dieses Plans.

---

## Task 1: Test-Infrastruktur aufsetzen

Im Projekt existieren bisher keine automatisierten Tests. Dieser Task legt
`pytest` + einen FastAPI-`TestClient` mit isolierter SQLite-Testdatenbank
pro Test an, als Basis für alle folgenden Tasks.

**Files:**
- Create: `essensplaner/requirements-dev.txt`
- Create: `essensplaner/tests/__init__.py`
- Create: `essensplaner/tests/conftest.py`
- Create: `essensplaner/tests/test_smoke.py`

**Interfaces:**
- Produces: Pytest-Fixture `client` (FastAPI `TestClient`, isolierte
  SQLite-Datei pro Test, Dependency-Override für `get_db`), nutzbar von
  allen folgenden API-Tests.

- [ ] **Step 1: `requirements-dev.txt` anlegen**

```
-r requirements.txt
pytest==8.3.3
```

- [ ] **Step 2: `tests/__init__.py` anlegen (leer)**

```python
```

- [ ] **Step 3: `conftest.py` schreiben**

```python
import os
import sys
from pathlib import Path

APP_DIR = Path(__file__).resolve().parent.parent / "app"
sys.path.insert(0, str(APP_DIR))
os.environ.setdefault("DB_PATH", str(Path(__file__).resolve().parent / "_unused.db"))

import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker

import database
import main as main_module


@pytest.fixture()
def client(tmp_path, monkeypatch):
    """FastAPI TestClient mit eigener, leerer SQLite-Datei pro Test."""
    db_file = tmp_path / "test.db"
    test_engine = create_engine(
        f"sqlite:///{db_file}", connect_args={"check_same_thread": False}
    )
    TestSessionLocal = sessionmaker(bind=test_engine, autoflush=False, autocommit=False)

    monkeypatch.setattr(database, "engine", test_engine)
    monkeypatch.setattr(database, "SessionLocal", TestSessionLocal)
    database.init_db()

    def override_get_db():
        db = TestSessionLocal()
        try:
            yield db
        finally:
            db.close()

    main_module.app.dependency_overrides[main_module.get_db] = override_get_db
    with TestClient(main_module.app) as test_client:
        yield test_client
    main_module.app.dependency_overrides.clear()
```

- [ ] **Step 4: Smoke-Test schreiben**

```python
def test_settings_endpoint_reachable(client):
    res = client.get("/api/settings")
    assert res.status_code == 200
```

- [ ] **Step 5: Dev-Dependencies installieren und Test laufen lassen**

Run: `cd essensplaner && pip install -r requirements-dev.txt`
Run: `cd essensplaner && python -m pytest tests/ -v`
Expected: `test_settings_endpoint_reachable` PASSED (Home-Assistant-Calls
schlagen lokal fehl und werden von `_list_entities_safe` bereits
abgefangen — siehe `app/main.py`).

- [ ] **Step 6: Commit**

```bash
git add essensplaner/requirements-dev.txt essensplaner/tests
git commit -m "test: pytest-Infrastruktur mit isoliertem TestClient aufsetzen"
```

---

## Task 2: Datenmodell — Offer, WatchlistItem, OfferSourceConfig, Settings-Erweiterung

**Files:**
- Modify: `essensplaner/app/models.py`
- Modify: `essensplaner/app/database.py` (`_migrate_add_missing_columns`)
- Modify: `essensplaner/app/schemas.py`
- Test: `essensplaner/tests/test_models.py`

**Interfaces:**
- Produces: SQLAlchemy-Modelle `Offer`, `WatchlistItem`, `OfferSourceConfig`
  (aus `models.py`); Pydantic-Schemas `OfferOut`, `WatchlistItemIn`,
  `WatchlistItemOut`, `OfferSourceConfigOut`, `OfferSourceConfigUpdateIn`
  (aus `schemas.py`); `Settings.plz`, `Settings.kaufland_store_url`,
  `Settings.edeka_store_url`.

- [ ] **Step 1: Modelle in `app/models.py` ergänzen**

Am Dateiende anfügen:

```python
class Offer(Base):
    """Ein einzelnes Angebot aus einem Connector-Lauf."""
    __tablename__ = "offers"

    id = Column(Integer, primary_key=True, index=True)
    retailer = Column(String, nullable=False)  # kaufland | edeka
    source = Column(String, nullable=False)  # kaufland_scraper | edeka_scraper | marktguru
    product_name = Column(String, nullable=False)
    description = Column(String, nullable=True)
    price = Column(Float, nullable=True)
    discount_text = Column(String, nullable=True)
    valid_from = Column(Date, nullable=False)
    valid_until = Column(Date, nullable=False)
    notified_at = Column(String, nullable=True)  # ISO-Timestamp oder NULL
    scraped_at = Column(String, nullable=False)  # ISO-Timestamp


class WatchlistItem(Base):
    """Artikel, die regelmäßig gebraucht werden, aber kein FridgeStaple sind
    (z.B. Mehl, Waschmittel) — ergänzt FridgeStaple für die Angebots-Hervorhebung."""
    __tablename__ = "watchlist_items"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String, nullable=False, unique=True)
    unit = Column(String, nullable=True)


class OfferSourceConfig(Base):
    """Pro Angebots-Quelle: aktiviert, Zeitplan, letzter Lauf."""
    __tablename__ = "offer_source_configs"

    id = Column(Integer, primary_key=True, index=True)
    source = Column(String, nullable=False, unique=True)  # kaufland_scraper | edeka_scraper | marktguru
    enabled = Column(Boolean, default=True)
    schedule_weekday = Column(Integer, nullable=True)  # 0=Montag..6=Sonntag
    schedule_hour = Column(Integer, nullable=True)  # 0-23
    last_run_at = Column(String, nullable=True)  # ISO-Timestamp
    last_status = Column(String, nullable=True)  # "ok" oder Fehlertext
```

`Date` ist bereits importiert (siehe `PlanEntry.date`); `notified_at`,
`scraped_at`, `last_run_at` bewusst als `String` (ISO-8601), damit der
gleiche Migrations-Mechanismus wie bei `Settings` (reines `ALTER TABLE ...
ADD COLUMN`, kein Typ-Mapping-Aufwand) für spätere Erweiterungen nutzbar
bleibt.

Bestehende `Settings`-Klasse erweitern (neue Zeilen nach `anthropic_api_key`):

```python
    plz = Column(String, nullable=True)
    kaufland_store_url = Column(String, nullable=True)
    edeka_store_url = Column(String, nullable=True)
```

- [ ] **Step 2: Migration in `app/database.py` ergänzen**

In `_migrate_add_missing_columns()`, nach dem bestehenden Block für
`anthropic_api_key`:

```python
        for column, coltype in [
            ("plz", "VARCHAR"),
            ("kaufland_store_url", "VARCHAR"),
            ("edeka_store_url", "VARCHAR"),
        ]:
            if column not in existing:
                conn.execute(text(f"ALTER TABLE settings ADD COLUMN {column} {coltype}"))
                conn.commit()
```

- [ ] **Step 3: Schemas in `app/schemas.py` ergänzen**

```python
class WatchlistItemIn(BaseModel):
    name: str
    unit: Optional[str] = None


class WatchlistItemOut(BaseModel):
    id: int
    name: str
    unit: Optional[str] = None

    class Config:
        from_attributes = True


class OfferOut(BaseModel):
    id: int
    retailer: str
    source: str
    product_name: str
    description: Optional[str] = None
    price: Optional[float] = None
    discount_text: Optional[str] = None
    valid_from: str
    valid_until: str
    matched_watchlist: bool = False
    matched_recipe_ids: list[int] = []

    class Config:
        from_attributes = True


class OfferSourceConfigOut(BaseModel):
    source: str
    enabled: bool
    schedule_weekday: Optional[int] = None
    schedule_hour: Optional[int] = None
    last_run_at: Optional[str] = None
    last_status: Optional[str] = None

    class Config:
        from_attributes = True


class OfferSourceConfigUpdateIn(BaseModel):
    enabled: Optional[bool] = None
    schedule_weekday: Optional[int] = None
    schedule_hour: Optional[int] = None
```

Diese Erweiterung von `SettingsIn`/`SettingsOut` (nicht neu, sondern
bestehende Klassen) übernimmt Task 10 zusammen mit dem zugehörigen
Endpoint, damit Modell- und Endpoint-Änderung an derselben Stelle
reviewt werden.

- [ ] **Step 4: Test schreiben, der die neuen Tabellen/Spalten prüft**

```python
from models import Offer, WatchlistItem, OfferSourceConfig, Settings


def test_new_tables_exist_and_settings_has_new_columns(client):
    import database
    from sqlalchemy import text

    with database.engine.connect() as conn:
        offer_cols = {row[1] for row in conn.execute(text("PRAGMA table_info(offers)"))}
        watchlist_cols = {row[1] for row in conn.execute(text("PRAGMA table_info(watchlist_items)"))}
        source_cols = {row[1] for row in conn.execute(text("PRAGMA table_info(offer_source_configs)"))}
        settings_cols = {row[1] for row in conn.execute(text("PRAGMA table_info(settings)"))}

    assert {"retailer", "source", "product_name", "valid_from", "valid_until"} <= offer_cols
    assert {"name", "unit"} <= watchlist_cols
    assert {"source", "enabled", "schedule_weekday", "schedule_hour"} <= source_cols
    assert {"plz", "kaufland_store_url", "edeka_store_url"} <= settings_cols
```

- [ ] **Step 5: Test laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_models.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add essensplaner/app/models.py essensplaner/app/database.py essensplaner/app/schemas.py essensplaner/tests/test_models.py
git commit -m "feat: Datenmodell für Angebote, Merkliste und Quellen-Konfiguration"
```

---

## Task 3: Watchlist-Endpunkte (CRUD)

Analog zum bestehenden `FridgeStaple`-Muster in `app/main.py`
(`/api/fridge/staples`).

**Files:**
- Modify: `essensplaner/app/main.py`
- Test: `essensplaner/tests/test_watchlist_api.py`

**Interfaces:**
- Consumes: `WatchlistItem` (Task 2), `WatchlistItemIn`/`WatchlistItemOut` (Task 2)
- Produces: `GET/POST /api/watchlist`, `DELETE /api/watchlist/{item_id}`

- [ ] **Step 1: Import ergänzen**

In `app/main.py`, Zeile mit `from models import ...`:

```python
from models import Recipe, Ingredient, PlanEntry, Settings, FridgeItem, FridgeStaple, WatchlistItem
```

Und bei `from schemas import (...)`:

```python
    WatchlistItemIn,
    WatchlistItemOut,
```

- [ ] **Step 2: Endpunkte ergänzen**

Neuer Abschnitt in `app/main.py` (z.B. nach dem Kühlschrank-Abschnitt):

```python
# ---------- Merkliste (regelmäßig benötigte Artikel, kein Kühlschrank-Bestand) ----------

@app.get("/api/watchlist", response_model=list[WatchlistItemOut])
def list_watchlist(db: Session = Depends(get_db)):
    return db.query(WatchlistItem).order_by(WatchlistItem.name).all()


@app.post("/api/watchlist", response_model=WatchlistItemOut)
def add_watchlist_item(payload: WatchlistItemIn, db: Session = Depends(get_db)):
    name = payload.name.strip()
    existing = db.query(WatchlistItem).filter(WatchlistItem.name.ilike(name)).first()
    if existing:
        existing.unit = payload.unit
        db.commit()
        db.refresh(existing)
        return existing
    item = WatchlistItem(name=name, unit=payload.unit)
    db.add(item)
    db.commit()
    db.refresh(item)
    return item


@app.delete("/api/watchlist/{item_id}")
def remove_watchlist_item(item_id: int, db: Session = Depends(get_db)):
    item = db.query(WatchlistItem).get(item_id)
    if not item:
        raise HTTPException(status_code=404, detail="Artikel nicht gefunden")
    db.delete(item)
    db.commit()
    return {"ok": True}
```

- [ ] **Step 3: Test schreiben**

```python
def test_watchlist_crud(client):
    res = client.post("/api/watchlist", json={"name": "Mehl", "unit": "kg"})
    assert res.status_code == 200
    item = res.json()
    assert item["name"] == "Mehl"

    res = client.get("/api/watchlist")
    assert res.status_code == 200
    assert len(res.json()) == 1

    res = client.delete(f"/api/watchlist/{item['id']}")
    assert res.status_code == 200
    assert client.get("/api/watchlist").json() == []


def test_remove_unknown_watchlist_item_returns_404(client):
    res = client.delete("/api/watchlist/9999")
    assert res.status_code == 404
```

- [ ] **Step 4: Tests laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_watchlist_api.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add essensplaner/app/main.py essensplaner/tests/test_watchlist_api.py
git commit -m "feat: CRUD-Endpunkte für die Merkliste hinzufügen"
```

---

## Task 4: Fuzzy-Matching-Modul

**Files:**
- Create: `essensplaner/app/offers/__init__.py`
- Create: `essensplaner/app/offers/matching.py`
- Modify: `essensplaner/requirements.txt`
- Test: `essensplaner/tests/test_offers_matching.py`

**Interfaces:**
- Produces: `match_score(a: str, b: str) -> float`,
  `find_matching_recipe_ids(product_name: str, db: Session, threshold: int = 80) -> list[int]`,
  `is_watchlist_match(product_name: str, db: Session, threshold: int = 80) -> bool`
  — beide nutzbar von Task 9 (Runner) und Task 14 (`GET /api/offers`).

- [ ] **Step 1: Dependency ergänzen**

In `essensplaner/requirements.txt`:

```
rapidfuzz==3.10.1
```

- [ ] **Step 2: `app/offers/__init__.py` anlegen (leer)**

```python
```

- [ ] **Step 3: `app/offers/matching.py` schreiben**

```python
"""Fuzzy-Matching zwischen Angebots-Produktnamen und Rezept-Zutaten /
Merklisten-Artikeln. Läuft on-demand (kein persistiertes Ergebnis), da sich
der Rezeptbestand ändern kann."""
from rapidfuzz import fuzz
from sqlalchemy.orm import Session

from models import Ingredient, FridgeStaple, WatchlistItem

DEFAULT_THRESHOLD = 80


def match_score(a: str, b: str) -> float:
    return fuzz.token_sort_ratio(a.lower().strip(), b.lower().strip())


def find_matching_recipe_ids(product_name: str, db: Session, threshold: int = DEFAULT_THRESHOLD) -> list[int]:
    matched: set[int] = set()
    for ingredient in db.query(Ingredient).all():
        if match_score(product_name, ingredient.name) >= threshold:
            matched.add(ingredient.recipe_id)
    return sorted(matched)


def is_watchlist_match(product_name: str, db: Session, threshold: int = DEFAULT_THRESHOLD) -> bool:
    names = [s.name for s in db.query(FridgeStaple).all()]
    names += [w.name for w in db.query(WatchlistItem).all()]
    return any(match_score(product_name, name) >= threshold for name in names)
```

- [ ] **Step 4: Test schreiben**

```python
from models import Recipe, Ingredient, FridgeStaple, WatchlistItem
from offers.matching import match_score, find_matching_recipe_ids, is_watchlist_match


def _db(client):
    import database
    return database.SessionLocal()


def test_match_score_tolerates_extra_words():
    assert match_score("Gouda Scheiben 250g", "Gouda") > 50


def test_find_matching_recipe_ids(client):
    db = _db(client)
    recipe = Recipe(title="Käsebrot")
    db.add(recipe)
    db.commit()
    db.add(Ingredient(recipe_id=recipe.id, name="Gouda", amount=200, unit="g"))
    db.commit()

    result = find_matching_recipe_ids("Gouda Scheiben 250g Angebot", db)
    assert result == [recipe.id]

    result_no_match = find_matching_recipe_ids("Klopapier", db)
    assert result_no_match == []


def test_is_watchlist_match_checks_staples_and_watchlist(client):
    db = _db(client)
    db.add(FridgeStaple(name="Milch"))
    db.add(WatchlistItem(name="Mehl"))
    db.commit()

    assert is_watchlist_match("Frische Milch 1L", db) is True
    assert is_watchlist_match("Weizenmehl Type 405", db) is True
    assert is_watchlist_match("Klopapier 8er Pack", db) is False
```

- [ ] **Step 5: Dependency installieren, Tests laufen lassen**

Run: `cd essensplaner && pip install -r requirements.txt`
Run: `cd essensplaner && python -m pytest tests/test_offers_matching.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add essensplaner/app/offers essensplaner/requirements.txt essensplaner/tests/test_offers_matching.py
git commit -m "feat: Fuzzy-Matching zwischen Angeboten und Zutaten/Merkliste"
```

---

## Task 5: Connector-Interface (`OfferData`)

**Files:**
- Create: `essensplaner/app/offers/base.py`
- Test: `essensplaner/tests/test_offers_base.py`

**Interfaces:**
- Produces: `@dataclass OfferData(retailer, product_name, description,
  price, discount_text, valid_from, valid_until)` — gemeinsamer
  Rückgabetyp aller drei Connectors (Task 6-8).

- [ ] **Step 1: `app/offers/base.py` schreiben**

```python
"""Gemeinsame Datenstruktur, die alle Angebots-Connectors zurückgeben.
Jeder Connector implementiert `fetch_offers(plz, store_url=None) -> list[OfferData]`."""
from dataclasses import dataclass
from datetime import date
from typing import Optional


@dataclass
class OfferData:
    retailer: str  # "kaufland" | "edeka"
    product_name: str
    valid_from: date
    valid_until: date
    description: Optional[str] = None
    price: Optional[float] = None
    discount_text: Optional[str] = None
```

- [ ] **Step 2: Test schreiben**

```python
from datetime import date
from offers.base import OfferData


def test_offer_data_holds_required_and_optional_fields():
    offer = OfferData(
        retailer="kaufland",
        product_name="Gouda 250g",
        valid_from=date(2026, 9, 1),
        valid_until=date(2026, 9, 7),
        price=1.99,
    )
    assert offer.retailer == "kaufland"
    assert offer.description is None
    assert offer.discount_text is None
```

- [ ] **Step 3: Test laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_offers_base.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add essensplaner/app/offers/base.py essensplaner/tests/test_offers_base.py
git commit -m "feat: gemeinsame OfferData-Struktur für alle Angebots-Connectors"
```

---

## Task 6: Kaufland-Scraper

**Files:**
- Create: `essensplaner/app/offers/kaufland_scraper.py`
- Create: `essensplaner/tests/fixtures/kaufland_sample.html`
- Modify: `essensplaner/requirements.txt`
- Test: `essensplaner/tests/test_kaufland_scraper.py`

**Interfaces:**
- Consumes: `OfferData` (Task 5)
- Produces: `SOURCE = "kaufland_scraper"`,
  `fetch_offers(plz: str, store_url: str | None = None) -> list[OfferData]`

Die konkreten CSS-Selektoren unten sind gegen die beigelegte Fixture
entwickelt (generische, plausible Struktur einer Angebots-Kachel-Liste).
**Bevor dieser Task als abgeschlossen gilt, muss zusätzlich gegen die
echte Kaufland-Angebotsseite geprüft werden** (Step 6) — das ist das in
der Spec vermerkte Scraper-Risiko, kein optionaler Schritt.

- [ ] **Step 1: Dependency ergänzen**

In `essensplaner/requirements.txt`:

```
beautifulsoup4==4.12.3
```

- [ ] **Step 2: Fixture-HTML anlegen**

`essensplaner/tests/fixtures/kaufland_sample.html`:

```html
<html><body>
  <div class="offer-tile" data-valid-from="2026-09-07" data-valid-until="2026-09-13">
    <h3 class="offer-tile__title">Gouda Scheiben 250g</h3>
    <p class="offer-tile__description">jung, mild</p>
    <span class="offer-tile__price">1,99 €</span>
  </div>
  <div class="offer-tile" data-valid-from="2026-09-07" data-valid-until="2026-09-13">
    <h3 class="offer-tile__title">Bio-Möhren 1kg</h3>
    <span class="offer-tile__discount">-20%</span>
  </div>
</body></html>
```

- [ ] **Step 3: `app/offers/kaufland_scraper.py` schreiben**

```python
"""Scraper für die öffentliche Kaufland-Angebotsseite der per PLZ ermittelten
Filiale. Fällt auf `store_url` zurück, falls die PLZ-Zuordnung fehlschlägt.
Bekanntes Risiko: Layout-Änderungen der Seite brechen die Selektoren unten
(siehe Spec, Abschnitt "Offene Risiken") — Fehler werden nie verschluckt,
sondern nach oben gereicht, damit der Runner (Task 9) sie in
OfferSourceConfig.last_status festhält."""
from datetime import date, datetime

import httpx
from bs4 import BeautifulSoup

from offers.base import OfferData

SOURCE = "kaufland_scraper"
_PLZ_LOOKUP_URL = "https://filiale.kaufland.de/angebote.html?plz={plz}"


def _parse_price(text: str | None) -> float | None:
    if not text:
        return None
    cleaned = text.replace("€", "").replace(",", ".").strip()
    try:
        return float(cleaned)
    except ValueError:
        return None


def _parse_offers_html(html: str) -> list[OfferData]:
    soup = BeautifulSoup(html, "html.parser")
    offers: list[OfferData] = []
    for tile in soup.select(".offer-tile"):
        title_el = tile.select_one(".offer-tile__title")
        if not title_el:
            continue
        valid_from = tile.get("data-valid-from")
        valid_until = tile.get("data-valid-until")
        if not valid_from or not valid_until:
            continue
        price_el = tile.select_one(".offer-tile__price")
        discount_el = tile.select_one(".offer-tile__discount")
        description_el = tile.select_one(".offer-tile__description")
        offers.append(OfferData(
            retailer="kaufland",
            product_name=title_el.get_text(strip=True),
            description=description_el.get_text(strip=True) if description_el else None,
            price=_parse_price(price_el.get_text() if price_el else None),
            discount_text=discount_el.get_text(strip=True) if discount_el else None,
            valid_from=date.fromisoformat(valid_from),
            valid_until=date.fromisoformat(valid_until),
        ))
    return offers


def fetch_offers(plz: str, store_url: str | None = None) -> list[OfferData]:
    url = store_url or _PLZ_LOOKUP_URL.format(plz=plz)
    response = httpx.get(url, timeout=15, follow_redirects=True)
    response.raise_for_status()
    return _parse_offers_html(response.text)
```

- [ ] **Step 4: Test schreiben (gegen Fixture, kein Live-Request)**

```python
from pathlib import Path
from offers.kaufland_scraper import _parse_offers_html

FIXTURE = (Path(__file__).parent / "fixtures" / "kaufland_sample.html").read_text()


def test_parse_offers_html_extracts_all_tiles():
    offers = _parse_offers_html(FIXTURE)
    assert len(offers) == 2
    assert offers[0].product_name == "Gouda Scheiben 250g"
    assert offers[0].price == 1.99
    assert offers[0].retailer == "kaufland"
    assert offers[1].discount_text == "-20%"
    assert offers[1].price is None


def test_parse_offers_html_skips_tiles_without_validity():
    html = '<div class="offer-tile"><h3 class="offer-tile__title">Ohne Datum</h3></div>'
    assert _parse_offers_html(html) == []
```

- [ ] **Step 5: Dependency installieren, Tests laufen lassen**

Run: `cd essensplaner && pip install -r requirements.txt`
Run: `cd essensplaner && python -m pytest tests/test_kaufland_scraper.py -v`
Expected: PASS

- [ ] **Step 6: Gegen die echte Seite verifizieren**

Mit dem Browser-Tool `https://filiale.kaufland.de/angebote.html` (bzw. die
tatsächliche Angebots-URL für die Ziel-PLZ) öffnen, die Seitenstruktur
inspizieren (`read_page` / DOM-Ausschnitt) und die CSS-Selektoren in
`_parse_offers_html` an die reale Struktur anpassen (Klassennamen ändern
sich vermutlich). Falls die Seite clientseitig gerendert wird (kein Angebot
im initialen HTML sichtbar), das im Task-Ergebnis vermerken — das wäre ein
Scope-Erweiterung (z.B. Playwright statt httpx), die nicht mehr in diesen
Plan fällt, sondern als neue Beobachtung/Folge-Task festgehalten wird.
Danach Fixture und Tests bei Bedarf an die reale Struktur anpassen.

- [ ] **Step 7: Commit**

```bash
git add essensplaner/app/offers/kaufland_scraper.py essensplaner/tests/fixtures/kaufland_sample.html essensplaner/tests/test_kaufland_scraper.py essensplaner/requirements.txt
git commit -m "feat: Kaufland-Angebots-Scraper hinzufügen"
```

---

## Task 7: Edeka-Scraper

Gleiches Muster wie Task 6, aber für Edeka. Edeka ist regional organisiert
— die PLZ-Zuordnung ist hier laut Spec am unsichersten, `store_url` als
Fallback entsprechend wichtiger.

**Files:**
- Create: `essensplaner/app/offers/edeka_scraper.py`
- Create: `essensplaner/tests/fixtures/edeka_sample.html`
- Test: `essensplaner/tests/test_edeka_scraper.py`

**Interfaces:**
- Consumes: `OfferData` (Task 5)
- Produces: `SOURCE = "edeka_scraper"`,
  `fetch_offers(plz: str, store_url: str | None = None) -> list[OfferData]`

- [ ] **Step 1: Fixture-HTML anlegen**

`essensplaner/tests/fixtures/edeka_sample.html`:

```html
<html><body>
  <li class="product-grid__item" data-from="2026-09-07" data-to="2026-09-13">
    <span class="product-grid__title">Rispentomaten 500g</span>
    <span class="product-grid__price">0,99 €</span>
  </li>
  <li class="product-grid__item" data-from="2026-09-07" data-to="2026-09-13">
    <span class="product-grid__title">Hähnchenbrustfilet 400g</span>
    <span class="product-grid__discount">-25%</span>
    <span class="product-grid__subtitle">aus Deutschland</span>
  </li>
</body></html>
```

- [ ] **Step 2: `app/offers/edeka_scraper.py` schreiben**

```python
"""Scraper für die öffentliche Edeka-Angebotsseite der per PLZ ermittelten
(regionalen) Filiale. Edeka ist dezentral organisiert — `store_url` als
Fallback ist hier wichtiger als bei Kaufland. Gleiche Fehlerbehandlungs-
Philosophie wie kaufland_scraper.py: Fehler werden nie verschluckt."""
from datetime import date

import httpx
from bs4 import BeautifulSoup

from offers.base import OfferData

SOURCE = "edeka_scraper"
_PLZ_LOOKUP_URL = "https://www.edeka.de/angebote/index.jsp?plz={plz}"


def _parse_price(text: str | None) -> float | None:
    if not text:
        return None
    cleaned = text.replace("€", "").replace(",", ".").strip()
    try:
        return float(cleaned)
    except ValueError:
        return None


def _parse_offers_html(html: str) -> list[OfferData]:
    soup = BeautifulSoup(html, "html.parser")
    offers: list[OfferData] = []
    for item in soup.select(".product-grid__item"):
        title_el = item.select_one(".product-grid__title")
        if not title_el:
            continue
        valid_from = item.get("data-from")
        valid_until = item.get("data-to")
        if not valid_from or not valid_until:
            continue
        price_el = item.select_one(".product-grid__price")
        discount_el = item.select_one(".product-grid__discount")
        subtitle_el = item.select_one(".product-grid__subtitle")
        offers.append(OfferData(
            retailer="edeka",
            product_name=title_el.get_text(strip=True),
            description=subtitle_el.get_text(strip=True) if subtitle_el else None,
            price=_parse_price(price_el.get_text() if price_el else None),
            discount_text=discount_el.get_text(strip=True) if discount_el else None,
            valid_from=date.fromisoformat(valid_from),
            valid_until=date.fromisoformat(valid_until),
        ))
    return offers


def fetch_offers(plz: str, store_url: str | None = None) -> list[OfferData]:
    url = store_url or _PLZ_LOOKUP_URL.format(plz=plz)
    response = httpx.get(url, timeout=15, follow_redirects=True)
    response.raise_for_status()
    return _parse_offers_html(response.text)
```

- [ ] **Step 3: Test schreiben**

```python
from pathlib import Path
from offers.edeka_scraper import _parse_offers_html

FIXTURE = (Path(__file__).parent / "fixtures" / "edeka_sample.html").read_text()


def test_parse_offers_html_extracts_all_items():
    offers = _parse_offers_html(FIXTURE)
    assert len(offers) == 2
    assert offers[0].product_name == "Rispentomaten 500g"
    assert offers[0].price == 0.99
    assert offers[0].retailer == "edeka"
    assert offers[1].discount_text == "-25%"
    assert offers[1].description == "aus Deutschland"


def test_parse_offers_html_skips_items_without_validity():
    html = '<li class="product-grid__item"><span class="product-grid__title">Ohne Datum</span></li>'
    assert _parse_offers_html(html) == []
```

- [ ] **Step 4: Tests laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_edeka_scraper.py -v`
Expected: PASS

- [ ] **Step 5: Gegen die echte Seite verifizieren**

Analog zu Task 6, Step 6: reale Edeka-Angebotsseite für eine Test-PLZ mit
dem Browser-Tool öffnen, Selektoren abgleichen/anpassen. Bei
Vollflächen-Ergebnis 0 Treffer gegen die reale Seite: `store_url`-Fallback
priorisieren (Edeka-PLZ-Zuordnung ist das größte Risiko laut Spec).

- [ ] **Step 6: Commit**

```bash
git add essensplaner/app/offers/edeka_scraper.py essensplaner/tests/fixtures/edeka_sample.html essensplaner/tests/test_edeka_scraper.py
git commit -m "feat: Edeka-Angebots-Scraper hinzufügen"
```

---

## Task 8: Marktguru-Connector

**Files:**
- Create: `essensplaner/app/offers/marktguru_connector.py`
- Create: `essensplaner/tests/fixtures/marktguru_sample.json`
- Test: `essensplaner/tests/test_marktguru_connector.py`

**Interfaces:**
- Consumes: `OfferData` (Task 5)
- Produces: `SOURCE = "marktguru"`,
  `fetch_offers(plz: str, store_url: str | None = None) -> list[OfferData]`
  (Parameter `store_url` wird hier nicht genutzt, bleibt aber für ein
  einheitliches Connector-Interface bestehen.)

Marktguru bietet keine offizielle, dokumentierte API — die Struktur unten
ist eine plausible Annahme (Angebots-Liste mit Name/Preis/Rabatt/
Gültigkeitszeitraum/Händlername) und muss in Step 5 gegen die echte
Antwort verifiziert werden.

- [ ] **Step 1: Fixture-JSON anlegen**

`essensplaner/tests/fixtures/marktguru_sample.json`:

```json
{
  "offers": [
    {
      "title": "Butter 250g",
      "retailer": "Edeka",
      "price": "1,49",
      "discount": "-15%",
      "valid_from": "2026-09-07",
      "valid_to": "2026-09-13"
    },
    {
      "title": "Kaffee gemahlen 500g",
      "retailer": "Kaufland",
      "price": "4,99",
      "discount": null,
      "valid_from": "2026-09-07",
      "valid_to": "2026-09-13"
    },
    {
      "title": "Waschmittel 20WL",
      "retailer": "Rewe",
      "price": "6,99",
      "discount": null,
      "valid_from": "2026-09-07",
      "valid_to": "2026-09-13"
    }
  ]
}
```

- [ ] **Step 2: `app/offers/marktguru_connector.py` schreiben**

```python
"""Fragt die inoffizielle Marktguru-API nach Angeboten für eine PLZ ab,
gefiltert auf die Händler Kaufland und Edeka. Inoffiziell/undokumentiert —
bewusst redundant zu den eigenen Scrapern (kaufland_scraper.py,
edeka_scraper.py), damit über mehrere Wochen verglichen werden kann,
welche Quelle zuverlässiger ist (siehe Spec)."""
from datetime import date

import httpx

from offers.base import OfferData

SOURCE = "marktguru"
_SEARCH_URL = "https://api.marktguru.de/api/v3/offers/search"
_RELEVANT_RETAILERS = {"kaufland": "kaufland", "edeka": "edeka"}


def _parse_price(text: str | None) -> float | None:
    if not text:
        return None
    try:
        return float(text.replace(",", "."))
    except ValueError:
        return None


def _parse_response(payload: dict) -> list[OfferData]:
    offers: list[OfferData] = []
    for raw in payload.get("offers", []):
        retailer_raw = (raw.get("retailer") or "").strip().lower()
        retailer = _RELEVANT_RETAILERS.get(retailer_raw)
        if retailer is None:
            continue
        valid_from = raw.get("valid_from")
        valid_to = raw.get("valid_to")
        if not valid_from or not valid_to:
            continue
        offers.append(OfferData(
            retailer=retailer,
            product_name=raw.get("title", "").strip(),
            price=_parse_price(raw.get("price")),
            discount_text=raw.get("discount"),
            valid_from=date.fromisoformat(valid_from),
            valid_until=date.fromisoformat(valid_to),
        ))
    return offers


def fetch_offers(plz: str, store_url: str | None = None) -> list[OfferData]:
    response = httpx.get(_SEARCH_URL, params={"postalCode": plz}, timeout=15)
    response.raise_for_status()
    return _parse_response(response.json())
```

- [ ] **Step 3: Test schreiben**

```python
import json
from pathlib import Path
from offers.marktguru_connector import _parse_response

FIXTURE = json.loads((Path(__file__).parent / "fixtures" / "marktguru_sample.json").read_text())


def test_parse_response_filters_to_kaufland_and_edeka():
    offers = _parse_response(FIXTURE)
    retailers = {o.retailer for o in offers}
    assert retailers == {"edeka", "kaufland"}
    assert len(offers) == 2


def test_parse_response_parses_price_and_discount():
    offers = _parse_response(FIXTURE)
    butter = next(o for o in offers if o.product_name == "Butter 250g")
    assert butter.price == 1.49
    assert butter.discount_text == "-15%"
    assert butter.retailer == "edeka"
```

- [ ] **Step 4: Tests laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_marktguru_connector.py -v`
Expected: PASS

- [ ] **Step 5: Gegen die echte API verifizieren**

Mit einem kurzen manuellen `httpx`/Browser-Request gegen
`https://api.marktguru.de/api/v3/offers/search?postalCode=<echte PLZ>`
(oder die tatsächlich erreichbare Marktguru-Such-URL, falls abweichend)
prüfen, ob Endpoint, Feldnamen und Auth-Anforderungen mit der Annahme oben
übereinstimmen; `_parse_response` und die Fixture entsprechend anpassen.
Falls ein API-Key/Session-Cookie nötig ist: als weiteres `Settings`-Feld
nachziehen (kleine additive Änderung, kein neuer Plan nötig).

- [ ] **Step 6: Commit**

```bash
git add essensplaner/app/offers/marktguru_connector.py essensplaner/tests/fixtures/marktguru_sample.json essensplaner/tests/test_marktguru_connector.py
git commit -m "feat: Marktguru-Connector als zweiten Angebots-Kanal hinzufügen"
```

---

## Task 9: Offer-Runner (Connector-Lauf persistieren)

Verbindet die drei Connectors mit der Datenbank: ruft `fetch_offers` auf,
ersetzt die alten `Offer`-Zeilen der jeweiligen Quelle, aktualisiert
`OfferSourceConfig.last_run_at`/`last_status`.

**Files:**
- Create: `essensplaner/app/offers/runner.py`
- Test: `essensplaner/tests/test_offers_runner.py`

**Interfaces:**
- Consumes: `OfferData` (Task 5), `Offer`/`OfferSourceConfig` (Task 2),
  `SOURCE`/`fetch_offers` aus den drei Connector-Modulen (Task 6-8)
- Produces: `run_source(source: str, db: Session, plz: str, store_url:
  str | None = None) -> OfferSourceConfig` — nutzt intern ein
  `CONNECTORS: dict[str, ModuleType]`-Mapping; wird von Task 10
  (manueller Refresh-Endpoint) und Task 11 (Scheduler) aufgerufen.

- [ ] **Step 1: `app/offers/runner.py` schreiben**

```python
"""Führt einen einzelnen Connector aus, ersetzt dessen alte Offer-Zeilen
und schreibt Erfolg/Fehler in OfferSourceConfig. Ein Lauf betrifft immer
nur die eigene `source` — andere Quellen bleiben unberührt."""
from datetime import datetime

from sqlalchemy.orm import Session

from models import Offer, OfferSourceConfig
from offers import kaufland_scraper, edeka_scraper, marktguru_connector

CONNECTORS = {
    kaufland_scraper.SOURCE: kaufland_scraper,
    edeka_scraper.SOURCE: edeka_scraper,
    marktguru_connector.SOURCE: marktguru_connector,
}


def get_or_create_source_config(source: str, db: Session) -> OfferSourceConfig:
    config = db.query(OfferSourceConfig).filter(OfferSourceConfig.source == source).first()
    if not config:
        config = OfferSourceConfig(source=source, enabled=True)
        db.add(config)
        db.commit()
        db.refresh(config)
    return config


def run_source(source: str, db: Session, plz: str, store_url: str | None = None) -> OfferSourceConfig:
    if source not in CONNECTORS:
        raise ValueError(f"Unbekannte Angebots-Quelle: {source}")

    config = get_or_create_source_config(source, db)
    connector = CONNECTORS[source]
    now = datetime.utcnow().isoformat()

    try:
        results = connector.fetch_offers(plz, store_url)
    except Exception as exc:
        config.last_run_at = now
        config.last_status = f"Fehler: {exc}"
        db.commit()
        db.refresh(config)
        return config

    db.query(Offer).filter(Offer.source == source).delete()
    for offer_data in results:
        db.add(Offer(
            retailer=offer_data.retailer,
            source=source,
            product_name=offer_data.product_name,
            description=offer_data.description,
            price=offer_data.price,
            discount_text=offer_data.discount_text,
            valid_from=offer_data.valid_from,
            valid_until=offer_data.valid_until,
            scraped_at=now,
        ))

    config.last_run_at = now
    config.last_status = "ok"
    db.commit()
    db.refresh(config)
    return config
```

- [ ] **Step 2: Test schreiben (Connector wird gemockt, kein echter Netzwerk-Call)**

```python
from datetime import date
from unittest.mock import patch

from models import Offer, OfferSourceConfig
from offers.base import OfferData
from offers.runner import run_source


def _db(client):
    import database
    return database.SessionLocal()


def test_run_source_persists_offers_and_marks_ok(client):
    db = _db(client)
    fake_offers = [
        OfferData(retailer="kaufland", product_name="Testartikel",
                   valid_from=date(2026, 9, 7), valid_until=date(2026, 9, 13), price=1.0)
    ]
    with patch("offers.kaufland_scraper.fetch_offers", return_value=fake_offers):
        config = run_source("kaufland_scraper", db, plz="12345")

    assert config.last_status == "ok"
    offers = db.query(Offer).filter(Offer.source == "kaufland_scraper").all()
    assert len(offers) == 1
    assert offers[0].product_name == "Testartikel"


def test_run_source_replaces_previous_offers_of_same_source(client):
    db = _db(client)
    old_offers = [OfferData(retailer="kaufland", product_name="Alt",
                             valid_from=date(2026, 9, 1), valid_until=date(2026, 9, 6))]
    new_offers = [OfferData(retailer="kaufland", product_name="Neu",
                             valid_from=date(2026, 9, 7), valid_until=date(2026, 9, 13))]

    with patch("offers.kaufland_scraper.fetch_offers", return_value=old_offers):
        run_source("kaufland_scraper", db, plz="12345")
    with patch("offers.kaufland_scraper.fetch_offers", return_value=new_offers):
        run_source("kaufland_scraper", db, plz="12345")

    names = [o.product_name for o in db.query(Offer).filter(Offer.source == "kaufland_scraper").all()]
    assert names == ["Neu"]


def test_run_source_records_failure_without_raising(client):
    db = _db(client)
    with patch("offers.kaufland_scraper.fetch_offers", side_effect=RuntimeError("Seite nicht erreichbar")):
        config = run_source("kaufland_scraper", db, plz="12345")

    assert "Fehler" in config.last_status
    assert db.query(Offer).filter(Offer.source == "kaufland_scraper").count() == 0
```

- [ ] **Step 3: Tests laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_offers_runner.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add essensplaner/app/offers/runner.py essensplaner/tests/test_offers_runner.py
git commit -m "feat: Offer-Runner verbindet Connectors mit Datenbank und Status-Tracking"
```

---

## Task 10: OfferSourceConfig-Endpunkte + manueller Refresh

**Files:**
- Modify: `essensplaner/app/main.py`
- Modify: `essensplaner/app/schemas.py` (`SettingsIn`/`SettingsOut` erweitern)
- Test: `essensplaner/tests/test_offers_sources_api.py`

**Interfaces:**
- Consumes: `run_source`, `CONNECTORS`, `get_or_create_source_config`
  (Task 9), `OfferSourceConfigOut`/`OfferSourceConfigUpdateIn` (Task 2)
- Produces: `GET /api/offers/sources`, `PUT /api/offers/sources/{source}`,
  `POST /api/offers/refresh/{source}`; `Settings.plz`/`store_url`-Felder
  in `SettingsIn`/`SettingsOut`.

- [ ] **Step 1: `SettingsIn`/`SettingsOut` in `app/schemas.py` erweitern**

```python
class SettingsIn(BaseModel):
    calendar_entity: str
    todo_entity: str
    anthropic_api_key: Optional[str] = None
    plz: Optional[str] = None
    kaufland_store_url: Optional[str] = None
    edeka_store_url: Optional[str] = None


class SettingsOut(BaseModel):
    calendar_entity: str
    todo_entity: str
    anthropic_api_key_set: bool = False
    plz: Optional[str] = None
    kaufland_store_url: Optional[str] = None
    edeka_store_url: Optional[str] = None
    available_calendars: list[EntityOption] = []
    available_todo_lists: list[EntityOption] = []
```

- [ ] **Step 2: `read_settings`/`save_settings` in `app/main.py` um die neuen Felder ergänzen**

In beiden `SettingsOut(...)`-Konstruktionsaufrufen (Zeilen ~79 und ~98)
ergänzen:

```python
        plz=settings.plz,
        kaufland_store_url=settings.kaufland_store_url,
        edeka_store_url=settings.edeka_store_url,
```

In `save_settings`, vor `db.commit()`:

```python
    if payload.plz is not None:
        settings.plz = payload.plz or None
    if payload.kaufland_store_url is not None:
        settings.kaufland_store_url = payload.kaufland_store_url or None
    if payload.edeka_store_url is not None:
        settings.edeka_store_url = payload.edeka_store_url or None
```

- [ ] **Step 3: Imports in `app/main.py` ergänzen**

```python
from models import (
    Recipe, Ingredient, PlanEntry, Settings, FridgeItem, FridgeStaple,
    WatchlistItem, OfferSourceConfig,
)
from schemas import (
    ...,
    OfferSourceConfigOut,
    OfferSourceConfigUpdateIn,
)
from offers.runner import run_source, get_or_create_source_config, CONNECTORS
```

- [ ] **Step 4: Endpunkte ergänzen**

```python
# ---------- Angebots-Quellen ----------

@app.get("/api/offers/sources", response_model=list[OfferSourceConfigOut])
def list_offer_sources(db: Session = Depends(get_db)):
    for source in CONNECTORS:
        get_or_create_source_config(source, db)
    return db.query(OfferSourceConfig).order_by(OfferSourceConfig.source).all()


@app.put("/api/offers/sources/{source}", response_model=OfferSourceConfigOut)
def update_offer_source(source: str, payload: OfferSourceConfigUpdateIn, db: Session = Depends(get_db)):
    if source not in CONNECTORS:
        raise HTTPException(status_code=404, detail="Unbekannte Angebots-Quelle")
    config = get_or_create_source_config(source, db)
    if payload.enabled is not None:
        config.enabled = payload.enabled
    if payload.schedule_weekday is not None:
        config.schedule_weekday = payload.schedule_weekday
    if payload.schedule_hour is not None:
        config.schedule_hour = payload.schedule_hour
    db.commit()
    db.refresh(config)
    return config


@app.post("/api/offers/refresh/{source}", response_model=OfferSourceConfigOut)
def refresh_offer_source(source: str, db: Session = Depends(get_db)):
    if source not in CONNECTORS:
        raise HTTPException(status_code=404, detail="Unbekannte Angebots-Quelle")
    settings = get_settings(db)
    if not settings.plz:
        raise HTTPException(status_code=400, detail="Bitte zuerst eine PLZ in den Einstellungen hinterlegen")
    store_url = {
        "kaufland_scraper": settings.kaufland_store_url,
        "edeka_scraper": settings.edeka_store_url,
    }.get(source)
    return run_source(source, db, plz=settings.plz, store_url=store_url)
```

- [ ] **Step 5: Test schreiben**

```python
from unittest.mock import patch
from datetime import date
from offers.base import OfferData


def test_list_offer_sources_returns_all_three(client):
    res = client.get("/api/offers/sources")
    assert res.status_code == 200
    sources = {s["source"] for s in res.json()}
    assert sources == {"kaufland_scraper", "edeka_scraper", "marktguru"}


def test_update_offer_source_toggles_enabled(client):
    res = client.put("/api/offers/sources/marktguru", json={"enabled": False})
    assert res.status_code == 200
    assert res.json()["enabled"] is False


def test_refresh_requires_plz(client):
    res = client.post("/api/offers/refresh/kaufland_scraper")
    assert res.status_code == 400


def test_refresh_runs_connector(client):
    client.post("/api/settings", json={"calendar_entity": "calendar.essensplan", "todo_entity": "todo.einkaufen", "plz": "12345"})
    fake_offers = [OfferData(retailer="kaufland", product_name="Test", valid_from=date(2026, 9, 7), valid_until=date(2026, 9, 13))]
    with patch("offers.kaufland_scraper.fetch_offers", return_value=fake_offers):
        res = client.post("/api/offers/refresh/kaufland_scraper")
    assert res.status_code == 200
    assert res.json()["last_status"] == "ok"


def test_refresh_unknown_source_returns_404(client):
    res = client.post("/api/offers/refresh/unknown")
    assert res.status_code == 404
```

- [ ] **Step 6: Tests laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_offers_sources_api.py -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add essensplaner/app/main.py essensplaner/app/schemas.py essensplaner/tests/test_offers_sources_api.py
git commit -m "feat: Endpunkte für Angebots-Quellen-Konfiguration und manuellen Refresh"
```

---

## Task 11: Scheduler (automatischer wöchentlicher Lauf pro Quelle)

**Files:**
- Create: `essensplaner/app/offers/scheduler.py`
- Modify: `essensplaner/app/main.py` (`on_startup`)
- Modify: `essensplaner/requirements.txt`
- Test: `essensplaner/tests/test_offers_scheduler.py`

**Interfaces:**
- Consumes: `run_source` (Task 9), `OfferSourceConfig` (Task 2)
- Produces: `start_scheduler() -> BackgroundScheduler`,
  `schedule_source(scheduler, config: OfferSourceConfig)` — aufgerufen aus
  `app/main.py`s `on_startup`.

- [ ] **Step 1: Dependency ergänzen**

In `essensplaner/requirements.txt`:

```
APScheduler==3.10.4
```

- [ ] **Step 2: `app/offers/scheduler.py` schreiben**

```python
"""Ein Job pro aktivierter Angebots-Quelle, Zeitplan aus OfferSourceConfig.
Läuft in-process im Add-on-Container (kein externer Cron nötig)."""
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.triggers.cron import CronTrigger

import database
from models import OfferSourceConfig
from offers.runner import run_source, get_or_create_source_config, CONNECTORS


def _job(source: str):
    from main import get_settings  # lazy Import, vermeidet Zirkularimport main<->scheduler
    db = database.SessionLocal()
    try:
        settings = get_settings(db)
        if not settings.plz:
            config = get_or_create_source_config(source, db)
            config.last_status = "Übersprungen: keine PLZ in den Einstellungen hinterlegt"
            db.commit()
            return
        store_url = {
            "kaufland_scraper": settings.kaufland_store_url,
            "edeka_scraper": settings.edeka_store_url,
        }.get(source)
        run_source(source, db, plz=settings.plz, store_url=store_url)
    finally:
        db.close()


def start_scheduler() -> BackgroundScheduler:
    scheduler = BackgroundScheduler()
    db = database.SessionLocal()
    try:
        for source in CONNECTORS:
            config = get_or_create_source_config(source, db)
            schedule_source(scheduler, config)
    finally:
        db.close()
    scheduler.start()
    return scheduler


def schedule_source(scheduler: BackgroundScheduler, config: OfferSourceConfig):
    job_id = f"offer_source_{config.source}"
    if scheduler.get_job(job_id):
        scheduler.remove_job(job_id)
    if not config.enabled:
        return
    weekday = config.schedule_weekday if config.schedule_weekday is not None else 6
    hour = config.schedule_hour if config.schedule_hour is not None else 3
    scheduler.add_job(
        _job,
        trigger=CronTrigger(day_of_week=weekday, hour=hour),
        args=[config.source],
        id=job_id,
        replace_existing=True,
    )
```

- [ ] **Step 3: In `app/main.py` an `on_startup` anschließen**

```python
from offers.scheduler import start_scheduler

...

@app.on_event("startup")
def on_startup():
    init_db()
    start_scheduler()
```

Zusätzlich: `PUT /api/offers/sources/{source}` (Task 10) soll den
laufenden Scheduler sofort aktualisieren. Dafür `app.state.scheduler`
setzen und in `update_offer_source` neu einplanen:

```python
@app.on_event("startup")
def on_startup():
    init_db()
    app.state.scheduler = start_scheduler()
```

In `update_offer_source`, nach `db.refresh(config)`:

```python
    from offers.scheduler import schedule_source
    schedule_source(app.state.scheduler, config)
```

- [ ] **Step 4: Test schreiben**

```python
from apscheduler.schedulers.background import BackgroundScheduler
from models import OfferSourceConfig
from offers.scheduler import schedule_source


def test_schedule_source_adds_job_when_enabled():
    scheduler = BackgroundScheduler()
    config = OfferSourceConfig(source="kaufland_scraper", enabled=True, schedule_weekday=6, schedule_hour=3)
    schedule_source(scheduler, config)
    assert scheduler.get_job("offer_source_kaufland_scraper") is not None
    scheduler.shutdown(wait=False)


def test_schedule_source_removes_job_when_disabled():
    scheduler = BackgroundScheduler()
    config = OfferSourceConfig(source="kaufland_scraper", enabled=True, schedule_weekday=6, schedule_hour=3)
    schedule_source(scheduler, config)
    config.enabled = False
    schedule_source(scheduler, config)
    assert scheduler.get_job("offer_source_kaufland_scraper") is None
    scheduler.shutdown(wait=False)


def test_startup_wires_scheduler_with_jobs_for_all_sources(client):
    from main import app
    scheduler = app.state.scheduler
    job_ids = {job.id for job in scheduler.get_jobs()}
    assert job_ids == {
        "offer_source_kaufland_scraper",
        "offer_source_edeka_scraper",
        "offer_source_marktguru",
    }
```

- [ ] **Step 5: Dependency installieren, Tests laufen lassen**

Run: `cd essensplaner && pip install -r requirements.txt`
Run: `cd essensplaner && python -m pytest tests/test_offers_scheduler.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add essensplaner/app/offers/scheduler.py essensplaner/app/main.py essensplaner/requirements.txt essensplaner/tests/test_offers_scheduler.py
git commit -m "feat: In-Process-Scheduler für automatische Connector-Läufe je Quelle"
```

---

## Task 12: HA-Benachrichtigung bei Merklisten-Treffern

**Files:**
- Modify: `essensplaner/app/ha_client.py`
- Modify: `essensplaner/app/offers/runner.py`
- Test: `essensplaner/tests/test_ha_client_notify.py`
- Test: `essensplaner/tests/test_offers_runner.py` (Erweiterung)

**Interfaces:**
- Consumes: `is_watchlist_match` (Task 4), `_post` (bestehend in
  `ha_client.py`)
- Produces: `ha_client.notify(message: str)`; `run_source` (Task 9) ruft
  nach erfolgreichem Lauf automatisch `notify(...)` auf, wenn neue
  Merklisten-Treffer vorliegen.

- [ ] **Step 1: `notify` in `app/ha_client.py` ergänzen**

```python
async def notify(message: str, title: str = "Essensplaner: Angebote"):
    """Sammel-Benachrichtigung über persistent_notification (kein Empfänger-
    Setup nötig, funktioniert ohne konfigurierte mobile_app-Integration)."""
    payload = {"title": title, "message": message, "notification_id": "essensplaner_offers"}
    return await _post("/api/services/persistent_notification/create", payload)
```

- [ ] **Step 2: `run_source` in `app/offers/runner.py` um Benachrichtigung erweitern**

Import ergänzen:

```python
import asyncio

from offers.matching import is_watchlist_match
import ha_client
```

Nach dem Einfügen der neuen `Offer`-Zeilen, vor dem finalen `db.commit()`
für `config`:

```python
    new_offers = db.query(Offer).filter(Offer.source == source, Offer.notified_at.is_(None)).all()
    matched = [o for o in new_offers if is_watchlist_match(o.product_name, db)]
    if matched:
        lines = [f"- {o.product_name}" + (f" ({o.discount_text})" if o.discount_text else "") for o in matched]
        message = f"{len(matched)} neue Angebote zu deiner Merkliste ({source}):\n" + "\n".join(lines)
        try:
            asyncio.run(ha_client.notify(message))
        except Exception:
            pass  # Benachrichtigung ist ein Nice-to-have, darf den Lauf nicht scheitern lassen
        for offer in matched:
            offer.notified_at = now
```

- [ ] **Step 3: Test für `notify` schreiben**

```python
from unittest.mock import AsyncMock, patch
import ha_client


async def _call():
    await ha_client.notify("3 neue Angebote")


def test_notify_calls_persistent_notification_service():
    with patch("ha_client._post", new_callable=AsyncMock) as mock_post:
        import asyncio
        asyncio.run(_call())
        mock_post.assert_called_once()
        args, kwargs = mock_post.call_args
        assert args[0] == "/api/services/persistent_notification/create"
        assert args[1]["notification_id"] == "essensplaner_offers"
```

- [ ] **Step 4: Runner-Test um Benachrichtigungs-Fall erweitern**

In `essensplaner/tests/test_offers_runner.py` ergänzen:

```python
from unittest.mock import AsyncMock, patch
from models import FridgeStaple


def test_run_source_notifies_and_marks_watchlist_matches(client):
    db = _db(client)
    db.add(FridgeStaple(name="Gouda"))
    db.commit()

    fake_offers = [OfferData(retailer="kaufland", product_name="Gouda Scheiben 250g",
                              valid_from=date(2026, 9, 7), valid_until=date(2026, 9, 13))]
    with patch("offers.kaufland_scraper.fetch_offers", return_value=fake_offers), \
         patch("ha_client.notify", new_callable=AsyncMock) as mock_notify:
        run_source("kaufland_scraper", db, plz="12345")

    mock_notify.assert_called_once()
    offer = db.query(Offer).filter(Offer.source == "kaufland_scraper").first()
    assert offer.notified_at is not None


def test_run_source_does_not_renotify_same_offer_on_next_run(client):
    db = _db(client)
    db.add(FridgeStaple(name="Gouda"))
    db.commit()
    fake_offers = [OfferData(retailer="kaufland", product_name="Gouda",
                              valid_from=date(2026, 9, 7), valid_until=date(2026, 9, 13))]

    with patch("offers.kaufland_scraper.fetch_offers", return_value=fake_offers), \
         patch("ha_client.notify", new_callable=AsyncMock) as mock_notify:
        run_source("kaufland_scraper", db, plz="12345")
        run_source("kaufland_scraper", db, plz="12345")

    # Zweiter Lauf ersetzt die Offer-Zeile komplett (neue Zeile ohne notified_at) -> erneuter Aufruf ist korrekt
    assert mock_notify.call_count == 2
```

Hinweis zum letzten Test: Da `run_source` alte Zeilen der Quelle komplett
löscht und neu anlegt (siehe Task 9), ist eine neu eingefügte, identische
Zeile in der DB nicht mehr dieselbe Zeile — `notified_at` startet jedes
Mal neu bei `NULL`. Das ist bewusst so einfach gehalten (kein
Deduplizieren über mehrere Wochen hinweg nötig, da ohnehin nur 1x/Woche
gelaufen wird); im Test wird das erwartete (aktuelle) Verhalten geprüft,
nicht eine striktere Wochen-Deduplizierung.

- [ ] **Step 5: Tests laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_ha_client_notify.py tests/test_offers_runner.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add essensplaner/app/ha_client.py essensplaner/app/offers/runner.py essensplaner/tests/test_ha_client_notify.py essensplaner/tests/test_offers_runner.py
git commit -m "feat: Sammel-Benachrichtigung bei neuen Angeboten zu Merklisten-Artikeln"
```

---

## Task 13: Planer-Integration (Gewichtsbonus für Angebots-Zutaten)

**Files:**
- Modify: `essensplaner/app/planner.py`
- Test: `essensplaner/tests/test_planner_offer_bonus.py`

**Interfaces:**
- Consumes: `find_matching_recipe_ids` (Task 4), `Offer` (Task 2)
- Produces: `generate_week_plan` berücksichtigt jetzt zusätzlich einen
  DB-Query auf aktive `Offer`-Zeilen; Signatur bleibt unverändert
  (`generate_week_plan(db, start_date)`).

- [ ] **Step 1: `app/planner.py` erweitern**

```python
from datetime import date as date_

from models import Recipe, PlanEntry, Offer
from offers.matching import find_matching_recipe_ids

FAVORITE_WEIGHT = 3
NORMAL_WEIGHT = 1
OFFER_WEIGHT_BONUS = 2


def _recipe_ids_with_active_offer(db: Session) -> set[int]:
    today = date_.today()
    active_offers = db.query(Offer).filter(Offer.valid_until >= today).all()
    matched: set[int] = set()
    for offer in active_offers:
        matched.update(find_matching_recipe_ids(offer.product_name, db))
    return matched
```

In `generate_week_plan`, nach `recipes = db.query(Recipe).all()`:

```python
    offer_recipe_ids = _recipe_ids_with_active_offer(db)
    weights = [
        (FAVORITE_WEIGHT if r.is_favorite else NORMAL_WEIGHT)
        + (OFFER_WEIGHT_BONUS if r.id in offer_recipe_ids else 0)
        for r in recipes
    ]
```

(ersetzt die bisherige Zeile
`weights = [FAVORITE_WEIGHT if r.is_favorite else NORMAL_WEIGHT for r in recipes]`)

`Session` muss zusätzlich importiert werden — ist in `planner.py` bereits
vorhanden (`from sqlalchemy.orm import Session`).

- [ ] **Step 2: Test schreiben**

```python
from datetime import date, timedelta
from models import Recipe, Ingredient, Offer
from planner import generate_week_plan, OFFER_WEIGHT_BONUS


def _db(client):
    import database
    return database.SessionLocal()


def test_recipe_with_active_offer_ingredient_gets_weight_bonus(client):
    db = _db(client)
    plain = Recipe(title="Ohne Angebot")
    with_offer = Recipe(title="Mit Angebot")
    db.add_all([plain, with_offer])
    db.commit()
    db.add(Ingredient(recipe_id=with_offer.id, name="Gouda", amount=200, unit="g"))
    db.add(Offer(
        retailer="kaufland", source="kaufland_scraper", product_name="Gouda Scheiben",
        valid_from=date.today(), valid_until=date.today() + timedelta(days=3),
        scraped_at=date.today().isoformat(),
    ))
    db.commit()

    import random
    random.seed(1)
    entries = generate_week_plan(db, date.today())
    # Kein deterministischer Ausgang bei zufälliger Auswahl -> stattdessen die
    # Gewichts-Hilfsfunktion direkt prüfen:
    from planner import _recipe_ids_with_active_offer
    assert with_offer.id in _recipe_ids_with_active_offer(db)
    assert plain.id not in _recipe_ids_with_active_offer(db)
    assert len(entries) == 7


def test_expired_offer_gives_no_bonus(client):
    db = _db(client)
    recipe = Recipe(title="Testgericht")
    db.add(recipe)
    db.commit()
    db.add(Ingredient(recipe_id=recipe.id, name="Gouda", amount=200, unit="g"))
    db.add(Offer(
        retailer="kaufland", source="kaufland_scraper", product_name="Gouda Scheiben",
        valid_from=date.today() - timedelta(days=10), valid_until=date.today() - timedelta(days=3),
        scraped_at=date.today().isoformat(),
    ))
    db.commit()

    from planner import _recipe_ids_with_active_offer
    assert recipe.id not in _recipe_ids_with_active_offer(db)
```

- [ ] **Step 3: Tests laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_planner_offer_bonus.py -v`
Expected: PASS

- [ ] **Step 4: Bestehendes Verhalten nicht gebrochen — vollen Testlauf prüfen**

Run: `cd essensplaner && python -m pytest tests/ -v`
Expected: alle bisherigen Tests weiterhin PASS

- [ ] **Step 5: Commit**

```bash
git add essensplaner/app/planner.py essensplaner/tests/test_planner_offer_bonus.py
git commit -m "feat: Gewichtsbonus für Rezepte mit aktuell rabattierten Zutaten"
```

---

## Task 14: `GET /api/offers` mit Matching-Infos

**Files:**
- Modify: `essensplaner/app/main.py`
- Test: `essensplaner/tests/test_offers_list_api.py`

**Interfaces:**
- Consumes: `Offer` (Task 2), `find_matching_recipe_ids`,
  `is_watchlist_match` (Task 4), `OfferOut` (Task 2)
- Produces: `GET /api/offers?retailer=&source=` → `list[OfferOut]`,
  sortiert nach `matched_watchlist` (True zuerst), dann `valid_until`.

- [ ] **Step 1: Import ergänzen**

```python
from models import Offer
from offers.matching import find_matching_recipe_ids, is_watchlist_match
```

(`Offer` ggf. der bestehenden `from models import (...)`-Zeile hinzufügen.)

- [ ] **Step 2: Endpunkt ergänzen**

```python
@app.get("/api/offers", response_model=list[OfferOut])
def list_offers(retailer: str | None = None, source: str | None = None, db: Session = Depends(get_db)):
    query = db.query(Offer)
    if retailer:
        query = query.filter(Offer.retailer == retailer)
    if source:
        query = query.filter(Offer.source == source)

    offers = query.all()
    results = []
    for offer in offers:
        results.append(OfferOut(
            id=offer.id,
            retailer=offer.retailer,
            source=offer.source,
            product_name=offer.product_name,
            description=offer.description,
            price=offer.price,
            discount_text=offer.discount_text,
            valid_from=offer.valid_from.isoformat(),
            valid_until=offer.valid_until.isoformat(),
            matched_watchlist=is_watchlist_match(offer.product_name, db),
            matched_recipe_ids=find_matching_recipe_ids(offer.product_name, db),
        ))
    results.sort(key=lambda o: (not o.matched_watchlist, o.valid_until))
    return results
```

- [ ] **Step 3: Test schreiben**

```python
from unittest.mock import patch
from datetime import date, timedelta
from offers.base import OfferData


def test_list_offers_empty_by_default(client):
    res = client.get("/api/offers")
    assert res.status_code == 200
    assert res.json() == []


def test_list_offers_sorts_watchlist_matches_first(client):
    client.post("/api/settings", json={"calendar_entity": "calendar.essensplan", "todo_entity": "todo.einkaufen", "plz": "12345"})
    client.post("/api/watchlist", json={"name": "Mehl"})

    fake_offers = [
        OfferData(retailer="kaufland", product_name="Klopapier 8er", valid_from=date.today(), valid_until=date.today() + timedelta(days=5)),
        OfferData(retailer="kaufland", product_name="Weizenmehl 1kg", valid_from=date.today(), valid_until=date.today() + timedelta(days=2)),
    ]
    with patch("offers.kaufland_scraper.fetch_offers", return_value=fake_offers):
        client.post("/api/offers/refresh/kaufland_scraper")

    res = client.get("/api/offers")
    assert res.status_code == 200
    body = res.json()
    assert len(body) == 2
    assert body[0]["product_name"] == "Weizenmehl 1kg"
    assert body[0]["matched_watchlist"] is True
    assert body[1]["matched_watchlist"] is False


def test_list_offers_filters_by_retailer_and_source(client):
    fake_offers = [OfferData(retailer="kaufland", product_name="Test", valid_from=date.today(), valid_until=date.today() + timedelta(days=5))]
    client.post("/api/settings", json={"calendar_entity": "calendar.essensplan", "todo_entity": "todo.einkaufen", "plz": "12345"})
    with patch("offers.kaufland_scraper.fetch_offers", return_value=fake_offers):
        client.post("/api/offers/refresh/kaufland_scraper")

    assert len(client.get("/api/offers?retailer=kaufland").json()) == 1
    assert len(client.get("/api/offers?retailer=edeka").json()) == 0
    assert len(client.get("/api/offers?source=kaufland_scraper").json()) == 1
    assert len(client.get("/api/offers?source=marktguru").json()) == 0
```

- [ ] **Step 4: Tests laufen lassen**

Run: `cd essensplaner && python -m pytest tests/test_offers_list_api.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add essensplaner/app/main.py essensplaner/tests/test_offers_list_api.py
git commit -m "feat: GET /api/offers mit Merklisten- und Rezept-Matching"
```

---

## Task 15: UI — Tab "Angebote"

**Files:**
- Modify: `essensplaner/app/static/index.html`

**Interfaces:**
- Consumes: `GET /api/offers`, `GET/PUT /api/offers/sources`,
  `POST /api/offers/refresh/{source}`, `GET/POST/DELETE /api/watchlist`,
  `GET/POST /api/settings` (Task 3, 10, 14)

Kein automatisierter Test für dieses statische-JS-Frontend (Projekt hat
dafür kein Test-Setup, siehe bestehende Tabs). Verifikation erfolgt manuell
in Task 16.

- [ ] **Step 1: Neuen Tab-Button ergänzen**

In `<nav class="tabs">` (Zeile ~280-285), nach dem Kühlschrank-Button:

```html
    <button data-tab="offers">Angebote</button>
```

- [ ] **Step 2: Neues Panel ergänzen**

Nach `</section>` von `tab-fridge` (vor `tab-ingredients`):

```html
  <section id="tab-offers" class="panel">
    <div class="card">
      <label for="offers-plz">Postleitzahl</label>
      <input type="text" id="offers-plz" placeholder="z.B. 12345">
      <label for="offers-kaufland-url">Kaufland-Filial-URL (Fallback, optional)</label>
      <input type="text" id="offers-kaufland-url" placeholder="https://...">
      <label for="offers-edeka-url">Edeka-Filial-URL (Fallback, optional)</label>
      <input type="text" id="offers-edeka-url" placeholder="https://...">
      <button id="offers-save-settings" class="primary">Speichern</button>
      <div id="offers-settings-msg" class="msg"></div>
    </div>

    <div class="card">
      <label for="watchlist-name">Merklisten-Artikel hinzufügen</label>
      <div class="ingredient-row">
        <input type="text" id="watchlist-name" class="ing-name" placeholder="Name">
        <input type="text" id="watchlist-unit" class="ing-unit" placeholder="Einheit (optional)">
      </div>
      <button id="watchlist-add" class="primary">Hinzufügen</button>
      <div id="watchlist-msg" class="msg"></div>
      <div id="watchlist-list"></div>
    </div>

    <div class="card">
      <p class="recipe-title">Quellen</p>
      <div id="offer-sources-list"></div>
    </div>

    <div class="toolbar">
      <select id="offers-filter-retailer">
        <option value="">Alle Händler</option>
        <option value="kaufland">Kaufland</option>
        <option value="edeka">Edeka</option>
      </select>
      <select id="offers-filter-source">
        <option value="">Alle Quellen</option>
        <option value="kaufland_scraper">Kaufland-Scraper</option>
        <option value="edeka_scraper">Edeka-Scraper</option>
        <option value="marktguru">Marktguru</option>
      </select>
    </div>
    <div id="offers-list"></div>
  </section>
```

- [ ] **Step 3: JS-Logik ergänzen**

Vor dem abschließenden `loadRecipes(); loadSettings(); loadFridge();`-Block
einfügen:

```javascript
    // ---------- Angebote ----------
    async function loadOfferSettings() {
      const res = await fetch('api/settings');
      if (!res.ok) return;
      const s = await res.json();
      document.getElementById('offers-plz').value = s.plz || '';
      document.getElementById('offers-kaufland-url').value = s.kaufland_store_url || '';
      document.getElementById('offers-edeka-url').value = s.edeka_store_url || '';
    }

    document.getElementById('offers-save-settings').addEventListener('click', async () => {
      const msg = document.getElementById('offers-settings-msg');
      const current = await (await fetch('api/settings')).json();
      try {
        const res = await fetch('api/settings', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            calendar_entity: current.calendar_entity,
            todo_entity: current.todo_entity,
            plz: document.getElementById('offers-plz').value.trim() || null,
            kaufland_store_url: document.getElementById('offers-kaufland-url').value.trim() || null,
            edeka_store_url: document.getElementById('offers-edeka-url').value.trim() || null,
          }),
        });
        if (!res.ok) throw new Error(await res.text());
        msg.textContent = 'Gespeichert.';
        msg.className = 'msg ok';
      } catch (e) {
        msg.textContent = 'Fehler: ' + e.message;
        msg.className = 'msg err';
      }
    });

    async function loadWatchlist() {
      const container = document.getElementById('watchlist-list');
      const res = await fetch('api/watchlist');
      const items = await res.json();
      if (!items.length) {
        container.innerHTML = '<p class="empty">Noch keine Merklisten-Artikel.</p>';
        return;
      }
      container.innerHTML = items.map(i => `
        <div class="card fridge-item">
          <p class="recipe-title">${escapeHtml(i.name)}</p>
          <p class="recipe-meta">${i.unit ? escapeHtml(i.unit) : '&nbsp;'}</p>
          <button class="secondary" data-remove-watchlist="${i.id}">Entfernen</button>
        </div>`).join('');
    }

    document.getElementById('watchlist-add').addEventListener('click', async () => {
      const nameInput = document.getElementById('watchlist-name');
      const unitInput = document.getElementById('watchlist-unit');
      const name = nameInput.value.trim();
      if (!name) return;
      await fetch('api/watchlist', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ name, unit: unitInput.value.trim() || null }),
      });
      nameInput.value = '';
      unitInput.value = '';
      loadWatchlist();
    });

    document.getElementById('watchlist-list').addEventListener('click', async (e) => {
      const btn = e.target.closest('[data-remove-watchlist]');
      if (!btn) return;
      await fetch(`api/watchlist/${btn.dataset.removeWatchlist}`, { method: 'DELETE' });
      loadWatchlist();
    });

    async function loadOfferSources() {
      const container = document.getElementById('offer-sources-list');
      const res = await fetch('api/offers/sources');
      const sources = await res.json();
      container.innerHTML = sources.map(s => `
        <div class="card fridge-item">
          <p class="recipe-title">${escapeHtml(s.source)}</p>
          <p class="recipe-meta">${s.enabled ? 'aktiv' : 'deaktiviert'} — zuletzt: ${escapeHtml(s.last_status || '-')}</p>
          <div class="fridge-actions">
            <button class="secondary" data-toggle-source="${s.source}" data-enabled="${s.enabled}">${s.enabled ? 'Deaktivieren' : 'Aktivieren'}</button>
            <button class="secondary" data-refresh-source="${s.source}">Jetzt aktualisieren</button>
          </div>
        </div>`).join('');
    }

    document.getElementById('offer-sources-list').addEventListener('click', async (e) => {
      const toggleBtn = e.target.closest('[data-toggle-source]');
      const refreshBtn = e.target.closest('[data-refresh-source]');
      if (toggleBtn) {
        const enabled = toggleBtn.dataset.enabled === 'true';
        await fetch(`api/offers/sources/${toggleBtn.dataset.toggleSource}`, {
          method: 'PUT',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ enabled: !enabled }),
        });
        loadOfferSources();
      } else if (refreshBtn) {
        refreshBtn.disabled = true;
        refreshBtn.textContent = 'Läuft...';
        try {
          await fetch(`api/offers/refresh/${refreshBtn.dataset.refreshSource}`, { method: 'POST' });
        } finally {
          loadOfferSources();
          loadOffers();
        }
      }
    });

    async function loadOffers() {
      const container = document.getElementById('offers-list');
      const retailer = document.getElementById('offers-filter-retailer').value;
      const source = document.getElementById('offers-filter-source').value;
      const params = new URLSearchParams();
      if (retailer) params.set('retailer', retailer);
      if (source) params.set('source', source);

      try {
        const res = await fetch('api/offers?' + params.toString());
        if (!res.ok) throw new Error(await res.text());
        const offers = await res.json();
        renderOffers(offers);
      } catch (e) {
        container.innerHTML = `<p class="empty">Fehler beim Laden: ${escapeHtml(e.message)}</p>`;
      }
    }

    function renderOffers(offers) {
      const container = document.getElementById('offers-list');
      if (!offers.length) {
        container.innerHTML = '<p class="empty">Keine Angebote gefunden. Lade oben eine Quelle manuell oder warte auf den nächsten geplanten Lauf.</p>';
        return;
      }
      container.innerHTML = offers.map(o => {
        const priceText = o.price != null ? `${o.price.toFixed(2)} €` : (o.discount_text || '');
        const watchlistTag = o.matched_watchlist ? '<span class="tag">Merkliste</span>' : '';
        return `
          <div class="card fridge-item ${o.matched_watchlist ? 'missing' : ''}">
            <p class="recipe-title">${escapeHtml(o.product_name)} <span class="recipe-meta">(${escapeHtml(o.retailer)})</span></p>
            <p class="recipe-meta">${escapeHtml(priceText)} · gültig bis ${escapeHtml(o.valid_until)} · Quelle: ${escapeHtml(o.source)}</p>
            ${watchlistTag}
          </div>`;
      }).join('');
    }

    document.getElementById('offers-filter-retailer').addEventListener('change', loadOffers);
    document.getElementById('offers-filter-source').addEventListener('change', loadOffers);

    loadOfferSettings();
    loadWatchlist();
    loadOfferSources();
    loadOffers();
```

- [ ] **Step 4: Commit**

```bash
git add essensplaner/app/static/index.html
git commit -m "feat: neuer UI-Tab 'Angebote' mit Merkliste, Quellen-Verwaltung und Filter"
```

---

## Task 16: Manuelle Verifikation gegen echte HA-Instanz

Kein neuer Code — dieser Task prüft die zusammengesetzte Funktionalität,
wie es die Spec unter "Testing" vorsieht (`run`-Skill/lokale Docker-Test-
Umgebung ist laut Projekt-Memory bereits vorhanden).

**Files:** keine (Verifikationsschritt)

- [ ] **Step 1: Backend lokal starten**

Run: `cd essensplaner/app && DB_PATH=./_local_test.db uvicorn main:app --reload --port 8099`

- [ ] **Step 2: Einstellungen befüllen**

Im Browser `http://localhost:8099` öffnen, Tab "Angebote", PLZ eintragen,
speichern.

- [ ] **Step 3: Manuellen Refresh pro Quelle auslösen**

Für jede der drei Quellen auf "Jetzt aktualisieren" klicken, `last_status`
in der UI prüfen. Bei Scraper-Fehlern: zurück zu Task 6/7, Selektoren
nachbessern.

- [ ] **Step 4: Merkliste + Hervorhebung prüfen**

Einen Artikel zur Merkliste hinzufügen, der zu einem echten aktuellen
Angebot passt (oder testweise einen `FridgeStaple`-Namen verwenden, der
zufällig zu einem Angebot passt) — prüfen, dass die Karte in der
Angebotsliste als "Merkliste" markiert oben erscheint.

- [ ] **Step 5: Benachrichtigung gegen echte HA-Instanz prüfen**

Add-on gegen die reale HA-Instanz laufen lassen (`SUPERVISOR_TOKEN`
gesetzt, wie im bestehenden Setup), einen Refresh mit einem
Merklisten-Treffer auslösen, prüfen, dass eine `persistent_notification`
in Home Assistant erscheint.

- [ ] **Step 6: Wochenplan-Bonus stichprobenhaft prüfen**

Mehrmals `POST /api/plan/generate` (bestehender Endpoint) aufrufen, bei
mind. einem aktiven Angebot mit passender Zutat beobachten, dass das
zugehörige Rezept spürbar häufiger gewählt wird als vergleichbare
Nicht-Angebots-Rezepte (kein exakter Test, nur Plausibilitätsprüfung, da
das Verhalten bereits in Task 13 automatisiert getestet ist).

- [ ] **Step 7: Ergebnis festhalten**

Kurz in der PR-Beschreibung/im Abschlussbericht notieren, ob die Live-
Selektoren für Kaufland/Edeka funktioniert haben oder ob eine
Folge-Beobachtung/-Task nötig ist (z.B. JS-Rendering, das httpx+
BeautifulSoup nicht erfasst).

---

## Task 17: Versionsbump + Changelog

**Files:**
- Modify: `essensplaner/config.yaml`
- Modify: `essensplaner/CHANGELOG.md`

CI (`.github/scripts/check_changelog.sh`) verlangt bei jeder Änderung
einen erhöhten `version`-Wert in `config.yaml` und einen passenden
`CHANGELOG.md`-Eintrag.

- [ ] **Step 1: Version in `config.yaml` erhöhen**

`version: "0.7.0"` → `version: "0.8.0"` (neues Feature, kein Breaking
Change).

- [ ] **Step 2: Changelog-Eintrag ergänzen**

In `essensplaner/CHANGELOG.md`, neuer Abschnitt oben nach der
Kopfzeile:

```markdown
## [0.8.0] - <Datum des Merges>

### Hinzugefügt

- Neuer Tab "Angebote": zeigt aktuelle Kaufland- und Edeka-Angebote
  (per PLZ), mit Hervorhebung von Standard- und Merklisten-Artikeln.
- Zwei parallele Datenquellen (eigener Scraper je Händler + Marktguru-API)
  zum Vergleich, einzeln konfigurierbar/deaktivierbar mit eigenem
  Zeitplan.
- Fuzzy-Matching zwischen Angeboten, Rezept-Zutaten und
  Standard-/Merklisten-Artikeln.
- Rezepte mit aktuell rabattierten Zutaten bekommen einen Gewichtsbonus
  in der automatischen Wochenplanung.
- Home-Assistant-Benachrichtigung bei neuen Angeboten zu
  Merklisten-Artikeln.
```

Platzhalter `<Datum des Merges>` beim tatsächlichen Merge durch das reale
Datum ersetzen (nicht vorab raten).

- [ ] **Step 3: Lokale CI-Prüfung nachvollziehen**

Run: `cd essensplaner && bash ../.github/scripts/check_changelog.sh` (falls
das Skript lokal ohne GitHub-Actions-Kontext lauffähig ist — sonst Diff
gegen `main` manuell mit dem Skript-Inhalt abgleichen: Version muss höher
sein als auf `main`, Changelog muss den neuen Versions-Header enthalten).

- [ ] **Step 4: Commit**

```bash
git add essensplaner/config.yaml essensplaner/CHANGELOG.md
git commit -m "chore: Version auf 0.8.0 anheben, Changelog für Angebote-Feature ergänzen"
```

---

## Self-Review-Notizen (bereits eingearbeitet)

- Spec-Abdeckung geprüft: Datenmodell (Task 2), Connectors (Task 6-8),
  Scheduling (Task 11), Matching (Task 4), Planer-Bonus (Task 13),
  Benachrichtigung (Task 12), UI (Task 15), API-Endpunkte (Task 3, 10, 14)
  — jeder Spec-Abschnitt hat einen Task.
- Kein "TBD"/Platzhalter außer dem in Task 17 bewusst offen gelassenen
  Merge-Datum (kann nicht vorab bekannt sein).
- Signaturen/Typen über Tasks hinweg geprüft: `fetch_offers(plz,
  store_url=None) -> list[OfferData]` konsistent in Task 5-9; `OfferOut`
  konsistent zwischen Task 2 und Task 14; `run_source(source, db, plz,
  store_url=None)` konsistent zwischen Task 9-12.
- Scraper-Tasks (6, 7) und der Marktguru-Task (8) enthalten explizit einen
  Live-Verifikations-Schritt statt stiller Annahmen — passt zum in der
  Spec benannten Risiko.
